# I301X — Qoder 项目指令

## 语言要求
**全程使用中文**：包括思考推理过程、代码注释、最终回复，全部使用简体中文。不得在任何环节使用英文思考或输出。

## 项目概况
本项目是基于 STM32H7 系列 MCU 的激光控制嵌入式系统，使用 STM32CubeMX 生成 HAL 库框架，开发环境为 MDK-ARM (Keil)。

## 芯片与平台
- **主控**: STM32H7 系列（双核架构，使用 L1-Cache，MPU 内存保护）
- **辅助芯片**: 后续可能涉及 STM32G474 系列
- **HAL 库**: 使用 STM32Cube HAL，必要时允许直接操作寄存器
- **IDE**: MDK-ARM (Keil)，项目文件在 `MDK-ARM/` 目录下（`.uvprojx`）

## 代码风格与规范

### 命名约定
- **函数名**: `模块_功能描述`，如 `bspInit()`, `dmx_func_0()`, `SGM5348_SetRGB()`
- **宏定义**: 全大写 + 下划线，如 `DMX_MAX_SLOTS`, `LOG_ENABLE_DMX`
- **结构体类型名**: 小写 + 下划线 + `_t` 后缀，如 `dmx_state_t`, `dmx_feature_t`
- **枚举类型名**：小写下划线分隔加 `_e` 后缀，如 `control_mode_e`, `filter_type_e`
- **全局变量**: 小写 + `g` 前缀或直接用下划线分隔，如 `gDmxReceiveData`, `dmx_laser`
- **头文件保护**: `__文件名_H__`，如 `__SGM5348_H__`, `__MYLOG_H__`
- **结构体成员**: 小写 + 下划线，如 `dmx_laser.pattern`, `dmx_rotate.enable`

### 模块组织
- 硬件驱动放 `Core/Src/bsp_*.c` + `Core/Inc/bsp_*.h`
- 芯片驱动放 `Core/Src/SGM5348.c` + `Core/Inc/SGM5348.h`
- 应用逻辑放 `Core/Src/m_task.c` + `Core/Inc/m_task.h`
- HAL 生成的代码在 `MX_*` 函数中，用户代码在 `USER CODE` 注释块之间

### 日志规范
- 使用项目自定义日志系统 `mylog.h`，不使用 `printf` 直接输出
- 日志模块标签：
  - `SYS` — 系统级
  - `DMX` — DMX512 协议
  - `LASER` — 激光控制
  - `NET` / `LSNET` — 网络相关
- 调试时使用 `LOG_*_DEBUG`，发布时通过 `LOG_ENABLE_*` 宏裁剪
- 临时调试打印用完及时删除或改为 LOG 宏

### 中断与回调
- DMX512 通过 UART 中断接收，使用 `HAL_UART_RxCpltCallback` 回调
- 定时器 IT 用于实时控制（频闪、旋转），周期在运行时动态计算

## 编码原则
1. **使用 HAL 库接口**，不绕过 HAL 直接操作寄存器，除非有性能或功能必要性（并加注释说明）
2. **中断回调尽量短**，只做数据搬运和标志位设置，耗时操作放主循环或任务函数
3. **外设初始化集中在 main.c 的 `MX_*_Init()` 中**，通过 CubeMX 管理
4. **不改动 CubeMX 自动生成的代码区域**（`USER CODE BEGIN/END` 之间除外）
5. **全局变量加注释说明用途和单位**，特别是物理量（如亮度 0-1000、角度 0-3600）
6. **浮点运算在 MCU 上代价高**，优先用整数 + 缩放
7. **编译开关控制功能**，如 `#ifndef DEBUG` 控制看门狗初始化
8. **错误处理**: 用 `Error_Handler()` 统一处理，陷入死循环并打印文件名+行号
9. **部分代码风格1:**对连续的同类赋值语句，默认执行对齐排版：`=` 应尽量保持纵向同列，适用于局部变量初始化、结构体成员赋值、寄存器字段赋值、配置字段赋值等同一语义块内的代码
10. **部分代码风格2:**对连续的宏定义，默认执行对齐排版：`#define` 后的宏名保持规整，宏值或表达式应尽量纵向同列；如带尾部注释，注释也应随整体排版保持整齐
11. **部分代码风格2:**该排版规则属于默认输出要求：当用户让 AI 编写或修改代码时，应主动遵守；前提是不破坏可读性、不导致行过长失控，也不与工程中已有格式化规则直接冲突

## 当前项目涉及的外设与协议
- **UART4**: DMX512 接收（9位模式，使用 RB8 判别地址/数据）
- **SPI2**: SGM5348 DAC 芯片通信
- **DAC1**: 模拟输出控制
- **I2C1**: EEPROM 存储
- **TIM2/3/6**: 定时器用于激光控制（旋转、频闪、点频）
- **DMA**: 配合 DMX 发送使用
- **独立看门狗 (IWDG)**: 系统异常复位

## 修改代码时的注意事项
- 修改 `.h` 文件时注意是否被多处包含，改动影响面
- DMX 通道函数表 `dmx_features[]` 的索引与起始通道号 `ch_start` 严格对应真实 DMX 地址
- `prj_status` 结构体涉及 EEPROM 存储，修改后需注意 `g_unFlashBuf` 的对齐和大小
- 注意，没有明确说明修改代码，优化代码等明确说明需要ai操作代码的文字，一律只做阅读文件操作，而不修改文件
- 没有明确说明在修改代码之后需要ai进行编译器验证操作的，ai不可以自行进入编译烧录等环节
- **代码输出格式**：回复中的代码块禁止添加行号前缀，保证用户可以直接复制粘贴使用
- 修改完代码之后要做一次简单的总结说明修改了哪些地方

## 后续补充
<!-- 用户可在此区域继续添加项目特殊约定、团队规则、构建脚本等 -->
